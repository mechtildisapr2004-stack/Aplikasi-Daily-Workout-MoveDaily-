const colors = {
  // Warna biru sporty sebagai identitas MoveDaily
  blue: (opacity = 1) => `rgba(28, 79, 223, ${opacity})`,
  green: (opacity = 1) => `rgba(34, 197, 94, ${opacity})`,
  white: (opacity = 1) => `rgba(255, 255, 255, ${opacity})`,
  black: (opacity = 1) => `rgba(18, 18, 18, ${opacity})`,
  grey: (opacity = 1) => `rgba(107, 114, 128, ${opacity})`,
};

export default colors;